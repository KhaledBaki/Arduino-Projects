# REMARKS
- Logic gates are usually made with transistors instead of buttons, but as of now I still haven't aquired any, therefore
I implemented the logic gates with buttons to get the same effect of an actual logic gate.
